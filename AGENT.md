# AGENT.md

## 목적
이 문서는 라오타 서버 저장소에서 AI 에이전트를 사용할 때의 작업 원칙과 현재 구현 상태를 함께 관리하는 기준 문서다.

기준 날짜: 2026-05-15

## AI 협업 원칙

### 1. 기본 자세
- LLM은 구현을 대신하는 자율 개발자가 아니라 스펙 정리, 계획 수립, 조사, 초안 작성, 리팩터링 보조에 사용하는 도구로 취급한다.
- 기본 구현 책임은 사람에게 있다.
- 작업 성격에 따라 전체 구현의 20%에서 100%까지 직접 코딩할 수 있다.

### 2. 구현 전에 하는 일
- 모델에 요청하기 전에 가능한 한 의사 코드, 인터페이스 초안, 데이터 흐름을 먼저 적는다.
- 요청과 생성 코드 사이의 거리를 짧게 유지한다.
- 요구사항이 애매하면 먼저 질문과 정리로 범위를 고정한다.

### 3. 모델을 쓰는 방식
- 임시 코드 생성기처럼 사용할 수 있다.
- 대화형 문서 편집기처럼 사용할 수 있다.
- 리서치 도구처럼 사용할 수 있다.
- 책임 있는 위임 도구처럼 사용하되, 질문하고, 반복하고, 리팩터링하고, 접근법을 더 명확하게 만드는 데 집중한다.

### 4. 코드 생성 제한
- 한 번에 사람이 리뷰 가능한 양보다 많은 코드를 생성하지 않는다.
- 생성량이 리뷰 용량을 넘기면 속도를 늦추고 작업을 더 작은 단위로 쪼갠다.
- 필요하면 사람이 직접 리팩터링해서 최종 결과를 전체적으로 이해 가능한 상태로 만든다.

### 5. 위임 금지선
- 직접 해본 적이 없거나, 혼자서는 구현·디버깅·운영할 수 없는 내용은 LLM이나 에이전트에 통째로 맡기지 않는다.
- 예외는 학습 목적의 실험이나 튜토리얼이며, 이런 결과물은 버릴 수 있다는 전제를 둔다.

### 6. 요약 원칙
- AI는 Star Trek의 Data가 아니라 Ship's Computer처럼 사용한다.
- 즉, 결정을 대체하지 않고 판단을 보조하는 방향으로 사용한다.

## 저장소 작업 규칙
- 구현 변경 전에는 관련 컨트롤러, 서비스, 저장소, 테스트를 함께 확인한다.
- 문서 업데이트 시에는 "계획"보다 "현재 코드에 실제로 존재하는 것"을 우선 기록한다.
- 큰 기능은 작은 커밋 단위로 쪼개고, 각 단계가 리뷰 가능해야 한다.
- 설명용 예시는 실제 서비스 코드와 분리한다.
- AI가 만든 초안은 그대로 머지하지 않고 사람이 구조와 예외 흐름을 이해한 뒤 정리한다.

## 현재 구현 상태

### 아키텍처
- Java 25, Spring Boot 4.0.2 기반 서버다.
- 도메인은 `auth`, `member`, `ramenShop`, `community`, `retrieval`, `global`, `admin`으로 나뉜다.
- 상태 변경과 조회 모두 JPA 기반으로 정리되어 있으며, 커뮤니티 조회도 JPA 쿼리 리포지토리로 동작한다.
- Flyway 마이그레이션과 Redis 캐시/토큰 저장소를 사용한다.
- 파일 업로드는 S3 호환 스토리지와 Cloudinary 라우팅 구성을 포함한다.

### 검색 / AI 인프라
- Spring AI 2.0.0-M6와 OpenAI 임베딩 모델을 사용한다.
- Oracle Vector Store를 별도 Oracle 데이터소스가 아니라 전용 `JdbcTemplate` 기반 수동 구성으로 연결한다.
- 운영 설정에서는 `OracleVectorStoreAutoConfiguration`을 제외하고 수동 `VectorStore` bean을 사용한다.
- `OracleVectorStoreSmokeTest`로 문서 저장/유사도 검색 스모크 테스트가 추가되어 있다.
- 검색/RAG 적재용 도메인 계약은 `com.raota.domain.retrieval.document` 패키지에서 관리한다.
- 현재 정의된 문서 타입은 `SHOP_PROFILE`, `SHOP_FACT`, `REVIEW_CHUNK`다.
- 현재 구현된 적재 뼈대는 라멘샵 프로필 문서 생성기와 샵 전체/단건 인덱싱 서비스다.

### 인증 및 보안
- OAuth2 로그인 흐름을 기준으로 JWT Access/Refresh 토큰을 발급한다.
- Refresh 토큰 재발급과 로그아웃 API가 구현되어 있다.
- Refresh 토큰 저장소는 JPA와 Redis 구현을 모두 가진다.
- `HttpCookieOAuth2AuthorizationRequestRepository`로 OAuth2 요청 상태를 쿠키에 저장한다.
- `LoginMemberArgumentResolver`, `JwtAuthenticationFilter`, `SecurityConfig`, 예외 처리기가 구성되어 있다.

### 라멘집
- 라멘집 목록 조회와 상세 조회 API가 있다.
- 상세 조회 응답은 로그인 사용자의 북마크 여부를 반영한다.
- 북마크 토글 API가 있다.
- 정보 오류 제보 API가 있다.
- 관리자용 라멘집 CRUD API와 관리 페이지 템플릿이 있다.

### 메뉴 투표
- 라멘집별 투표 현황 조회 API가 있다.
- 메뉴 투표는 토글/변경 흐름을 지원한다.
- 투표 데이터는 `is_cancelled` 기반 소프트 딜리트 정책을 사용한다.

### 인증샷 및 파일
- 인증샷 등록, 목록 조회, 삭제 API가 있다.
- 업로드는 `/files/upload-ticket`으로 업로드 티켓을 발급받는 방식이다.
- 파일 용도별 디렉터리 라우팅(`PROFILE`, `BACKGROUND`, `SHOP`, `COMMUNITY`, `PROOF`)이 구현되어 있다.
- 삭제 시 실제 스토리지 오브젝트 정리 로직이 연결되어 있다.

### 라멘로그
- 기존 `tb_ramen_proof_picture` 테이블을 `tb_ramen_log`로 승격해 기존 인증샷 데이터를 보존한다.
- `/ramen-logs` 리소스를 제공하며, 기존 `/photos` API도 같은 라멘로그 데이터를 읽고 쓰는 호환 API로 유지한다.
- 공개 피드의 최신순·인기순 정렬, 가게·키워드 필터, 페이지네이션을 지원한다.
- 라멘로그 작성, 상세 조회, 수정, 소프트 삭제와 공개·비공개 설정을 지원한다.
- 국물·면·간·토핑 취향 태그와 재방문 의사를 기록한다.
- 좋아요 토글과 로그인 사용자 기준 `liked`, `mine` 응답을 제공한다.
- 내 로그와 사용자 공개 로그, 로그가 존재하는 가게 필터 목록 API를 제공한다.
- 프로필 통계는 기존 `total_photo_count`와 별도로 `total_log_count`를 제공한다.

### 커뮤니티
- 게시글 목록, 상세, 생성, 수정, 삭제 API가 있다.
- 게시글은 소프트 딜리트를 사용한다.
- 게시글 좋아요 토글 API가 있다.
- 좋아요 3개 이상인 게시글을 인기글로 간주하며, 인기글 목록과 최근 인기글 조회 API가 있다.
- 상세 조회는 로그인 사용자의 `isLiked` 상태를 포함한다.
- 커뮤니티 글 작성 시 연결 가능한 라멘집 옵션 조회 API가 있다.

### 댓글
- 댓글 생성, 수정, 삭제 API가 있다.
- 게시글별 댓글 조회는 부모 댓글 페이지네이션 후 답글을 묶는 스레드 구조다.
- 답글(Depth 1) 구조를 지원한다.
- 댓글도 소프트 딜리트를 사용한다.

### 마이페이지 / 회원
- 내 프로필 조회 및 수정 API가 있다.
- 내 인증샷, 북마크, 방문 기록, 작성 글, 작성 댓글 조회 API가 있다.
- 다른 사용자 기준의 프로필/활동 조회 API도 제공한다.
- 프로필 완료 여부를 신규 회원 판단 기준으로 사용하는 흐름이 반영되어 있다.

### 캐시 및 공통 인프라
- `RamenShopCacheService`와 글로벌 캐시 설정이 있다.
- Redis 기반 캐시 무효화 퍼블리셔/리스너 구성이 있다.
- 공통 응답 포맷 `ApiResponse`, `PageResponse`, 전역 예외 처리기가 있다.
- Discord 로깅 appender 구성이 포함되어 있다.

## 테스트 현황
- 단위 테스트: 인증, 댓글, 게시글 서비스 등 핵심 도메인 로직 검증 파일이 있다.
- 웹/보안 테스트: JWT 필터, 로그인 멤버 리졸버, 쿠키 매니저, 보안 설정 테스트가 있다.
- 통합 테스트: `Auth`, `Community`, `Member`, `RamenShop`, `Root`, Flyway 마이그레이션 검증이 있다.
- Testcontainers 기반 `BaseIntegrationTest`로 MySQL/Redis 환경을 공통화한다.

## 문서 운영 규칙
- 새 기능이나 정책 변경 시 이 문서의 "AI 협업 원칙"과 "현재 구현 상태"를 함께 갱신한다.
- 구현 예정이지만 아직 코드에 없는 내용은 완료 기능처럼 적지 않는다.
- 큰 설계 변경이 생기면 `README.md`의 사용자 대상 설명과도 맞춘다.
