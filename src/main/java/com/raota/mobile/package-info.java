/**
 * 모바일 앱 기준 v2 도메인(`/api/v2`, `tb_v2_*`)의 모듈을 모아 두는 패키지다.
 *
 * <p>이 패키지 자체는 모듈이 아니며, 하위 도메인 패키지를 각각 모듈로 선언한다.
 * web 패키지를 참조하지 않는다. v1 데이터가 필요한 이관 코드는 web 클래스 대신 v1 테이블을 직접 읽는다.</p>
 */
package com.raota.mobile;
