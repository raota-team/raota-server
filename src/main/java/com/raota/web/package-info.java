/**
 * 기존 웹 서비스(v1)의 도메인 모듈을 모아 두는 패키지다.
 *
 * <p>이 패키지 자체는 모듈이 아니며, 하위의 account, community, ramenlog, ramenshop이 각각 모듈이다.
 * v1 종료 시 이 패키지를 통째로 제거한다. mobile 패키지를 참조하지 않는다.</p>
 */
package com.raota.web;
